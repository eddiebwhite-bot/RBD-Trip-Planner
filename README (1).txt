If you saw "File not found":
- These links are regenerated now. If your chat client blocked earlier downloads, try again here.
- This build also removes template literals and adds an inline favicon to avoid the earlier errors.

Quick test:
1) Open index.html, click Load Google Maps.
2) Enter Pickup & Dropoff → Get Quote (see distance, time, estimated total).
3) (Optional) Use in Planner → add stops → Optimize Route → Open in Google Maps.
